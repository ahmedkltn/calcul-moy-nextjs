import { Container,Row} from "react-bootstrap";
import Branche from "./Branche";
interface Props {
    parcours : string;
    abbr:string;
    semestre:string;
}
export default ({parcours,abbr,semestre} : Props)=>{
      <Container fluid>
        <Row className="justify-content-md-center mt-4">
        </Row>
      </Container>;
}